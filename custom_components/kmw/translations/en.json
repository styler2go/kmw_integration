{
  "config": {
    "abort": {
      "already_configured": "This API key is already configured."
    },
    "error": {
      "no_data": "Could not get forecast data. Please verify your API key and internet connection.",
      "invalid_api_key": "The API key you entered is not valid."
    },
    "step": {
      "user": {
        "data": {
          "api_key": "API Key",
          "name": "Name"
        },
        "description": "This integration fetches weather data from the Kachelmann Wetter API service. Enter your API key below. After setup, add locations as devices."
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "name": "Name",
          "current_weather": "Which data shall be used as the current weather?",
          "forecast": "Do you want to have forecast data (recommended)?",
          "model": "Forecast model"
        }
      }
    }
  },
  "config_subentries": {
    "location": {
      "entry_type": "Location",
      "initiate_flow": {
        "user": "Add location",
        "reconfigure": "Reconfigure location"
      },
      "abort": {
        "already_configured": "This zone is already configured."
      },
      "error": {
        "zone_not_found": "The selected zone was not found."
      },
      "step": {
        "add_location": {
          "data": {
            "zone": "Zone",
            "name": "Name (optional, overrides zone name)",
            "latitude": "Latitude (optional, overrides zone)",
            "longitude": "Longitude (optional, overrides zone)"
          },
          "description": "Select a Home Assistant zone as base location. You can optionally override the name and coordinates."
        },
        "reconfigure_location": {
          "data": {
            "zone": "Zone",
            "name": "Name (optional, overrides zone name)",
            "latitude": "Latitude (optional, overrides zone)",
            "longitude": "Longitude (optional, overrides zone)"
          },
          "description": "Change the zone or override name and coordinates."
        }
      }
    }
  }
}
